<Window x:Class="DrJaw.Views.User.LomIn"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DrJaw.Views.User" WindowStartupLocation="CenterOwner"
        mc:Ignorable="d"
        Title="LomIn" Height="228" Width="426">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Вес (г):" VerticalAlignment="Center" Width="80"/>
            <TextBox x:Name="textBoxWeight" Width="200" />
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Цена/г:" VerticalAlignment="Center" Width="80"/>
            <TextBox x:Name="textBoxPricePerGramm" Width="200" />
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Итого:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="textBoxTotalPrice" Width="120" IsReadOnly="True"/>
            <Button x:Name="buttonAction" Content="Сохранить" Width="120" Height="32" Margin="12,0,0,0"
              IsDefault="True" Click="Action"/>
            <Button Content="Отмена" Width="100" Height="32" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>

    </Grid>
</Window>
