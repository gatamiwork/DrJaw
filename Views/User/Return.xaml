<Window x:Class="DrJaw.Views.User.Return"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:DrJaw.Views.User" WindowStartupLocation="CenterOwner" 
        mc:Ignorable="d"
        Title="Return" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Дата:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker x:Name="DatePickerReturn"
                  SelectedDateChanged="DatePickerReturn_SelectedDateChanged"/>
            <TextBlock x:Name="TextCount" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
        <DataGrid x:Name="DataGridReturnItems" Grid.Row="1"
              AutoGenerateColumns="False" IsReadOnly="True"
              EnableRowVirtualization="True" EnableColumnVirtualization="True"
              SelectionMode="Single" SelectionChanged="DataGridReturnItems_SelectionChanged">
            <!-- Подставь свои колонки -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Артикул" Binding="{Binding Articul}" Width="*"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="120"/>
                <DataGridTextColumn Header="Дата продажи" Binding="{Binding SoldAt}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="ButtonReturn" Content="Сделать возврат" Width="160" Height="32"
              IsDefault="True" Click="ButtonReturn_Click" IsEnabled="False"/>
            <Button Content="Закрыть" Width="120" Height="32" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
