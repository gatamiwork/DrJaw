<Window x:Class="DrJaw.Views.User.TransferOut"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DrJaw.Views.User"
        mc:Ignorable="d"
        Title="TransferOut" Height="220" Width="460">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Товар к отправке:" FontWeight="Bold"/>
            <TextBlock x:Name="TextItemInfo" TextWrapping="Wrap"
                       Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
            <TextBlock Text="Магазин-получатель:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="comboboxTransfer"
                      Width="260"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      IsSynchronizedWithCurrentItem="False"/>
        </StackPanel>
        <Separator Grid.Row="2" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button x:Name="ButtonTransfer"
                    Content="Отправить"
                    Width="120" Height="32"
                    IsDefault="True"
                    Click="Action"/>
            <Button Content="Отмена"
                    Width="100" Height="32" Margin="8,0,0,0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
