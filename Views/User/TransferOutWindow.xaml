<Window x:Class="DrJaw.Views.User.TransferOutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Перемещение в другой магазин"
        Height="520" Width="820"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ВЕРХНЯЯ ПАНЕЛЬ: выбор магазина -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Магазин назначения:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Width="280"
                  ItemsSource="{Binding TargetMarts}"
                  SelectedItem="{Binding SelectedTargetMart, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  DisplayMemberPath="Name"/>
            <TextBlock Text="{Binding TotalSelectedText}"
                   Margin="16,0,0,0"
                   VerticalAlignment="Center"
                   Foreground="#666"/>
        </StackPanel>

        <!-- СПИСОК (вытянутые карточки по всей ширине) -->
        <ListView Grid.Row="1"
              ItemsSource="{Binding Items}"
              BorderThickness="0"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              VirtualizingPanel.ScrollUnit="Pixel"
              ScrollViewer.CanContentScroll="True">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin"  Value="0,6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment"   Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="8"
                        Background="#FFFFFF"
                        BorderBrush="#E5E5E5"
                        BorderThickness="1"
                        Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="108"/>
                                <!-- превью -->
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                                <!-- детали -->
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="260"/>
                                <!-- qty + цена -->
                            </Grid.ColumnDefinitions>

                            <!-- Превью -->
                            <Border Grid.Column="0" Width="96" Height="96" Background="#FAFAFA" CornerRadius="6">
                                <Grid>
                                    <Image Source="{Binding Thumbnail}"
                                       Stretch="Uniform"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                    <TextBlock Text="Нет изображения"
                                           Foreground="#888" FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>

                            <!-- Детали -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Articul}" FontSize="16" FontWeight="SemiBold" />
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding Metal}" Foreground="#666"/>
                                    <TextBlock Text="  •  " Foreground="#AAA"/>
                                    <TextBlock Text="Доступно: " Foreground="#666"/>
                                    <TextBlock Text="{Binding MaxQuantity}" Foreground="#666"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Количество + цена -->
                            <DockPanel Grid.Column="4" LastChildFill="False" VerticalAlignment="Center">
                                <!-- Кол-во -->
                                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                    <Button Content="−" Width="36" Height="36" Margin="0,0,6,0"
                                        Command="{Binding DecrementQuantityCommand}"/>
                                    <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"
                                         Width="64" Height="36"
                                         HorizontalContentAlignment="Center"
                                         VerticalContentAlignment="Center"
                                         Margin="0,0,6,0"/>
                                    <Button Content="+" Width="36" Height="36"
                                        Command="{Binding IncrementQuantityCommand}"/>
                                </StackPanel>

                                <!-- Цена/итог -->
                                <StackPanel Orientation="Vertical" Margin="16,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Price, StringFormat={}{0:N2}}"
                                           FontSize="14" Foreground="#333" />
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="Итого: " Foreground="#666"/>
                                        <TextBlock Text="{Binding Quantity}" Foreground="#666"/>
                                        <TextBlock Text=" × " Foreground="#666"/>
                                        <TextBlock Text="{Binding Price, StringFormat={}{0:N2}}" Foreground="#666"/>
                                    </StackPanel>
                                </StackPanel>
                            </DockPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- НИЖНЯЯ ПАНЕЛЬ -->
        <DockPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="{Binding TotalSelectedText}"
                   VerticalAlignment="Center" Foreground="#666"
                   DockPanel.Dock="Left"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button Content="Переместить" MinWidth="140" Height="36" Margin="0,0,8,0"
                    IsDefault="True"
                    Command="{Binding TransferCommand}"/>
                <Button Content="Закрыть" MinWidth="120" Height="36"
                    IsCancel="True"
                    Command="{Binding CancelCommand}"/>
            </StackPanel>
        </DockPanel>
    </Grid>

</Window>
