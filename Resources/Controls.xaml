<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- TextBox: MD “Outlined”. Если нет ключа — используй MaterialDesignTextBox -->
    <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="Margin" Value="{StaticResource App.Control.Margin}"/>
        <Setter Property="Padding" Value="{StaticResource App.Control.Padding}"/>
        <Setter Property="MinHeight" Value="{StaticResource App.Control.MinHeight}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- PasswordBox -->
    <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignOutlinedPasswordBox}">
        <Setter Property="Margin" Value="{StaticResource App.Control.Margin}"/>
        <Setter Property="Padding" Value="{StaticResource App.Control.Padding}"/>
        <Setter Property="MinHeight" Value="{StaticResource App.Control.MinHeight}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- Button: приподнятая кнопка -->
    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Margin" Value="{StaticResource App.Control.Margin}"/>
        <Setter Property="Padding" Value="{StaticResource App.Control.Padding}"/>
        <Setter Property="MinHeight" Value="{StaticResource App.Control.MinHeight}"/>
        <Setter Property="MinWidth"  Value="{StaticResource App.Button.MinWidth}"/>
    </Style>

    <!-- ComboBox -->
    <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignComboBox}">
        <Setter Property="Margin" Value="{StaticResource App.Control.Margin}"/>
        <Setter Property="MinHeight" Value="{StaticResource App.Control.MinHeight}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- DataGrid: MD-стиль + твои дефолты -->
    <Style TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
        <Setter Property="Margin" Value="{StaticResource App.Control.Margin}"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="SelectionMode" Value="Extended"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
    </Style>

</ResourceDictionary>
